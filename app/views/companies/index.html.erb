<h1><i class="bi bi-buildings-fill"></i>Empresas</h1>

<div><%= link_to "Nova empresa", new_company_path%></div>


<div id="companies">
  <% @companies.each do |company| %>
    <div>
      <a href="/companies/<%= company.id %>">
        <%= company.name %>
      </a>
    </div>
  <% end %>
</div>